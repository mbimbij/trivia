name: E2E nightly execution

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'

jobs:
  Run:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'
          cache-dependency-path: 'java/pom.xml' # optional
      - uses: browser-actions/setup-chrome@v1
      - run: chrome --version
      - name: Maven Install
        working-directory: java
        run: make mvn-install
        env:
          PORT_OPENAPI_MAVEN_GENERATION: ${{ vars.PORT_OPENAPI_MAVEN_GENERATION }}
      - name: Ensure browsers are installed
        working-directory: java
        run: ./mvnw exec:java -e -pl e2e-tests -D exec.mainClass=com.microsoft.playwright.CLI -D exec.args="install-deps" --no-transfer-progress
<<<<<<< Updated upstream
      - uses: nick-fields/retry@v3
        name: Execute E2E tests
        with:
          timeout_minutes: 40
          max_attempts: 3
          command: cd java && make e2e-tests-prod
=======
      - name: Execute E2E tests
        uses: nick-fields/retry@v3
        working-directory: java
        with:
          timeout_minutes: 30
          max_attempts: 3
          command: make e2e-tests-prod
>>>>>>> Stashed changes
        env:
          QA_USER_ID: ${{ secrets.QA_USER_ID }}
          QA_USER_EMAIL: ${{ secrets.QA_USER_EMAIL }}
          QA_USER_PASSWORD: ${{ secrets.QA_USER_PASSWORD }}
          HOSTNAME_PROD: ${{ vars.HOSTNAME_PROD }}
      - name: Upload Playwright screenshots
        if: failure()  # Upload only if the test fails (optional)
        uses: actions/upload-artifact@v3
        with:
          name: playwright-screenshots
          path: java/e2e-tests/screenshots/
